import React from 'react';
import {formatDate} from "../../utils/date";
import {Button} from "./TimeButton.styles";

const TimeButton = ({showId}) => {

    return (
        <Button
            type="default"
        >
        </Button>
    );
};

export default TimeButton;